# FlowSense — development & test dependencies.
# Includes the edge stack so tests that import flowsense.detector / stream /
# storage.garage can run locally. Pin all versions (PEP 440 compliant).

-r requirements-edge.txt

# Test runner
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-mock==3.14.0
respx==0.22.0
httpx[http2]==0.28.1
aiohttp==3.11.0
fakeredis[lua]==2.26.0
faker==30.3.0

# Formatting / linting / typing
black==24.10.0
isort==5.13.2
ruff==0.6.9
mypy==1.13.0
coverage[toml]==7.6.0
