# FlowSense — shared base dependencies (pinned for reproducible builds).
# See requirements-edge.txt / requirements-api.txt / requirements-dev.txt for
# component-specific splits. The `pip install -r requirements-<component>.txt`
# files include this base transitively via their own pins.

# Web framework
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.20

# Database & ORM (async PostgreSQL)
sqlalchemy==2.0.51
asyncpg==0.31.0
alembic==1.14.0
geoalchemy2==0.20.0
psycopg[binary]==3.2.0

# Auth & security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
PyJWT==2.8.0

# Config / HTTP
python-dotenv==1.2.2
httpx==0.28.1
requests==2.32.3

# S3 / object storage
boto3==1.38.0
aioboto3==14.0.0
moto[sqs,ses]==5.0.0

# SUMO simulation
traci==1.27.1
sumolib==1.27.1

# Utilities
click==8.4.2
PyYAML==6.0.3
pydantic==2.13.4
