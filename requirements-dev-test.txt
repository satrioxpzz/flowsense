# Test Dependencies - Required for pytest, CI/CD validation runs!
pytest==8.3.x                # Modern async test runner (asyncpg compatible)  
pytest-asyncio>=0.24        # Async tests in FastAPI applications 
httpx[app]>=0.27           # ASGI client for testing endpoints locally
respx                      # Mock http:// requests within pytest fixtures
coverage[toml]==7.x         # Code coverage reports per commit
faker==31.4                 # Test data generation (fake cities, addresses)

# Development & Debugging Tools  
black>=24.x                 # Python code formatter for 90-char limits  
isort>=5.13                 # Sort imports by standard library/3rd-party/local  
mypy[reports]>=1.8          # Static type checking with strictness levels
ruff>=0.6                   # Fast linting tool (replacement: flake8+pylint)

# Async debugging & profiling 
aiohttp                    # HTTP server testing client for async endpoints
pytest-mock==3.x           # Mock fixtures and patch modules in tests  
fakeredis[lua]==2.19       # In-memory Redis cache for local dev/CI jobs  
