[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "flowsense"
version = "0.1.0"
description = "FlowSense edge vehicle detection system"
dependencies = []

[tool.setuptools.packages.find]
include = ["flowsense*"]
